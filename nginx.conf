server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Gzip
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml text/javascript image/svg+xml;
    gzip_min_length 256;

    # Cache des assets statiques (fichiers avec hash dans le nom)
    # IMPORTANT: Cette location doit être AVANT le fallback SPA
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        # Ne pas fallback vers index.html si le fichier n'existe pas
        try_files $uri =404;
    }

    # index.html ne doit JAMAIS être mis en cache (les assets hashés le sont déjà)
    location = /index.html {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        add_header Pragma "no-cache";
        add_header Expires "0";
    }

    # SPA fallback — toutes les routes renvoient index.html
    # Sauf /assets/ qui est géré ci-dessus
    location / {
        try_files $uri $uri/ /index.html;
    }
}
